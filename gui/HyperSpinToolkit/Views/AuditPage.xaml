<Page x:Class="HyperSpinToolkit.Views.AuditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:HyperSpinToolkit.Controls"
    xmlns:effects="clr-namespace:HyperSpinToolkit.Effects"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" Title="Audit" Background="Transparent" d:DesignHeight="900" d:DesignWidth="1060">

    <Grid>
        <Border Style="{DynamicResource BackgroundLayerStyle}" />
        <effects:ParticleCanvas Preset="AmbientStars" MaxParticles="30" SpawnRate="0.5" />

        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="28,20">
            <StackPanel>

                <!-- Header -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="ECOSYSTEM AUDIT" Style="{DynamicResource ArcadeTitleStyle}" FontSize="22"/>
                        <TextBlock Text="PAC-MAN COMPLETION METERS" Style="{DynamicResource ArcadeLabelStyle}" Margin="0,4,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                        <Button Content="FULL AUDIT" Style="{DynamicResource ArcadeButtonGreenStyle}" Padding="14,8" FontSize="11" Command="{Binding ViewModel.RunFullAuditCommand}" Margin="0,0,8,0" IsEnabled="{Binding ViewModel.IsLoading, Converter={StaticResource InverseBoolConverter}}"/>
                        <Button Content="EMULATORS" Style="{DynamicResource ArcadeButtonStyle}" Padding="14,8" FontSize="11" Command="{Binding ViewModel.AuditEmulatorsCommand}" IsEnabled="{Binding ViewModel.IsLoading, Converter={StaticResource InverseBoolConverter}}"/>
                    </StackPanel>
                </Grid>

                <ProgressBar Value="{Binding ViewModel.AuditProgress}" Maximum="100" Style="{DynamicResource NeonProgressBarGreenStyle}" Margin="0,0,0,16" Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <!-- Summary cards (Neon arcade style) -->
                <UniformGrid Rows="1" Columns="4" Margin="0,0,0,24">

                    <Border Style="{DynamicResource ArcadeCardStyle}" Margin="0,0,10,0">
                        <StackPanel HorizontalAlignment="Center">
                            <controls:ScoreCounter TargetValue="{Binding ViewModel.Summary.TotalSystems}" DigitFontSize="28" Height="36" Width="140" />
                            <TextBlock Text="SYSTEMS" Style="{DynamicResource ArcadeLabelStyle}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{DynamicResource ArcadeCardGreenStyle}" Margin="0,0,10,0">
                        <StackPanel HorizontalAlignment="Center">
                            <controls:ScoreCounter TargetValue="{Binding ViewModel.Summary.TotalRoms}" DigitFontSize="28" Height="36" Width="140" />
                            <TextBlock Text="TOTAL ROMS" Style="{DynamicResource ArcadeLabelStyle}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{DynamicResource ArcadeCardPinkStyle}" Margin="0,0,10,0">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="{Binding ViewModel.Summary.HealthyEmulators, StringFormat='{}{0}/{1}'}" Style="{DynamicResource PixelTextStyle}" FontSize="28" HorizontalAlignment="Center"/>
                            <TextBlock Text="HEALTHY EMUS" Style="{DynamicResource ArcadeLabelStyle}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{DynamicResource ArcadeCardStyle}" Margin="0">
                        <StackPanel HorizontalAlignment="Center">
                            <controls:NeonGauge Value="{Binding ViewModel.Summary.HealthScoreValue}" Maximum="100" Width="70" Height="70" GaugeMode="Circular" ShowPercentage="True" StrokeThickness="5" />
                            <TextBlock Text="HEALTH" Style="{DynamicResource ArcadeLabelStyle}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                </UniformGrid>

                <!-- Single system audit -->
                <Border Style="{DynamicResource ArcadeCardStyle}" Margin="0,0,0,24">
                    <StackPanel>
                        <TextBlock Text="AUDIT SINGLE SYSTEM" Style="{DynamicResource ArcadeSubtitleStyle}" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Style="{DynamicResource ArcadeTextBoxStyle}" Text="{Binding ViewModel.SpecificSystem, UpdateSourceTrigger=PropertyChanged}" Tag="System name (e.g. MAME, Nintendo 64)"/>
                            <Button Grid.Column="1" Margin="10,0,0,0" Content="AUDIT" Style="{DynamicResource ArcadeButtonPinkStyle}" Padding="16,8" Command="{Binding ViewModel.RunSystemAuditCommand}" IsEnabled="{Binding ViewModel.IsLoading, Converter={StaticResource InverseBoolConverter}}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Systems results table -->
                <TextBlock Text="SYSTEMS" Style="{DynamicResource ArcadeSubtitleStyle}" Margin="0,0,0,10"/>
                <Border Style="{DynamicResource ArcadeCardStyle}" Padding="0" Margin="0,0,0,24">
                    <DataGrid ItemsSource="{Binding ViewModel.Systems}" Style="{DynamicResource ArcadeDataGridStyle}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" RowHeight="38" CanUserResizeRows="False" Background="Transparent" BorderThickness="0" MaxHeight="380">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="System" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="ROMs" Binding="{Binding RomCount}" Width="70"/>
                            <DataGridTextColumn Header="XML" Binding="{Binding XmlGameCount}" Width="70"/>
                            <DataGridTextColumn Header="Matched" Binding="{Binding MatchedGames}" Width="80"/>
                            <DataGridTextColumn Header="Missing" Binding="{Binding MissingRoms}" Width="75"/>
                            <DataGridTextColumn Header="Extra" Binding="{Binding ExtraRoms}" Width="65"/>
                            <DataGridTextColumn Header="Issues" Binding="{Binding IssueCount}" Width="65"/>
                            <DataGridTemplateColumn Header="Health" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding HealthDisplay}" Foreground="{Binding HealthColor, Converter={StaticResource HexColorToBrushConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- Emulators table -->
                <TextBlock Text="EMULATORS" Style="{DynamicResource ArcadeSubtitleStyle}" Margin="0,0,0,10" Visibility="{Binding ViewModel.Emulators.Count, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <Border Style="{DynamicResource ArcadeCardStyle}" Padding="0" Margin="0,0,0,24" Visibility="{Binding ViewModel.Emulators.Count, Converter={StaticResource BoolToVisibilityConverter}}">
                    <DataGrid ItemsSource="{Binding ViewModel.Emulators}" Style="{DynamicResource ArcadeDataGridStyle}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column" RowHeight="38" CanUserResizeRows="False" Background="Transparent" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Emulator" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Exes" Binding="{Binding ExeCount}" Width="60"/>
                            <DataGridTextColumn Header="Files" Binding="{Binding FileCount}" Width="70"/>
                            <DataGridTextColumn Header="Size (MB)" Binding="{Binding TotalSizeMb, StringFormat='{}{0:N0}'}" Width="90"/>
                            <DataGridTemplateColumn Header="Healthy" Width="75">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding HealthDisplay}" Foreground="{Binding HealthColor, Converter={StaticResource HexColorToBrushConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="15"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <TextBlock Text="{Binding ViewModel.StatusText}" Style="{DynamicResource ConsoleTextStyle}" FontSize="11" Margin="0,0,0,8" HorizontalAlignment="Right"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>

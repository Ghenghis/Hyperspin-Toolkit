<ui:Page x:Class="HyperSpinToolkit.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:controls="clr-namespace:HyperSpinToolkit.Controls"
    xmlns:effects="clr-namespace:HyperSpinToolkit.Effects"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" Title="Settings" Background="Transparent" d:DesignHeight="800" d:DesignWidth="900">

    <Grid>
        <Border Style="{DynamicResource BackgroundLayerStyle}" />

        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="28,20">
            <StackPanel MaxWidth="700" HorizontalAlignment="Left">

                <TextBlock Text="SETTINGS" Style="{DynamicResource ArcadeTitleStyle}" FontSize="22" Margin="0,0,0,24"/>

                <!-- Python / Bridge -->
                <TextBlock Text="PYTHON BACKEND" Style="{DynamicResource ArcadeSubtitleStyle}" Margin="0,0,0,10"/>
                <Border Style="{DynamicResource ArcadeCardStyle}" Margin="0,0,0,20">
                    <StackPanel Spacing="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Python Executable:" VerticalAlignment="Center"/>
                            <ui:TextBox Grid.Column="1" Text="{Binding ViewModel.PythonExe, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="python  (or full path)"/>
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Toolkit Root:" VerticalAlignment="Center"/>
                            <ui:TextBox Grid.Column="1" Text="{Binding ViewModel.ToolkitRoot, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Opacity="0.7"/>
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Config Path:" VerticalAlignment="Center"/>
                            <ui:TextBox Grid.Column="1" Text="{Binding ViewModel.ConfigPath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Opacity="0.7"/>
                        </Grid>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="RECONNECT BRIDGE" Style="{DynamicResource ArcadeButtonGreenStyle}" Padding="14,6" Command="{Binding ViewModel.ReconnectBridgeCommand}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding ViewModel.BridgeStatus}" FontSize="12" Opacity="0.7"/>
                        <TextBlock Text="{Binding ViewModel.StatusText}" FontSize="12" Foreground="{StaticResource SystemAccentColorPrimaryBrush}"/>
                    </StackPanel>
                </Border>

                <!-- Available tools -->
                <TextBlock Text="MCP TOOL DISCOVERY" Style="{DynamicResource ArcadeSubtitleStyle}" Margin="0,0,0,10"/>
                <Border Style="{DynamicResource ArcadeCardGreenStyle}" Margin="0,0,0,20">
                    <StackPanel Spacing="12">
                        <Button Content="LIST AVAILABLE TOOLS" Style="{DynamicResource ArcadeButtonStyle}" Padding="14,6" Command="{Binding ViewModel.ListToolsCommand}"/>
                        <ScrollViewer MaxHeight="320" VerticalScrollBarVisibility="Auto">
                            <TextBlock Text="{Binding ViewModel.AvailableTools}" FontFamily="Consolas" FontSize="12" Opacity="0.8" TextWrapping="Wrap" Visibility="{Binding ViewModel.AvailableTools, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </ScrollViewer>
                    </StackPanel>
                </Border>

                <!-- Gamepad / Input -->
                <TextBlock Text="GAMEPAD &amp; TRANSITIONS" Style="{DynamicResource ArcadeSubtitleStyle}" Margin="0,0,0,10"/>
                <Border Style="{DynamicResource ArcadeCardStyle}" Margin="0,0,0,20">
                    <StackPanel Spacing="12">
                        <StackPanel Orientation="Horizontal">
                            <controls:LedIndicator x:Name="GamepadLed" LedState="Off" LabelText="GAMEPAD" LedSize="12" Margin="0,0,16,0" />
                            <TextBlock x:Name="GamepadStatusText" Text="No gamepad detected" Style="{DynamicResource ConsoleTextStyle}" FontSize="11" VerticalAlignment="Center" />
                        </StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Transition Style:" VerticalAlignment="Center"/>
                            <ComboBox x:Name="TransitionPicker" MinWidth="180" VerticalAlignment="Center">
                                <ComboBoxItem Content="Pixel Dissolve" Tag="PixelDissolve" IsSelected="True" />
                                <ComboBoxItem Content="Slide Left" Tag="SlideLeft" />
                                <ComboBoxItem Content="Slide Right" Tag="SlideRight" />
                                <ComboBoxItem Content="Fade Through" Tag="FadeThrough" />
                                <ComboBoxItem Content="Neon Wipe" Tag="NeonWipe" />
                            </ComboBox>
                        </Grid>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Transition Speed:" VerticalAlignment="Center"/>
                            <Slider x:Name="TransitionSpeedSlider" Minimum="150" Maximum="800" Value="400" Width="200" HorizontalAlignment="Left" />
                        </Grid>
                        <CheckBox x:Name="VibrationCheck" Content="Enable vibration feedback" IsChecked="True" />
                        <StackPanel Orientation="Horizontal">
                            <Button Content="SAVE CONFIG" Style="{DynamicResource ArcadeButtonGreenStyle}" Padding="14,6" Margin="0,0,8,0" Click="OnSaveGamepadConfig" />
                            <Button Content="RESET DEFAULTS" Style="{DynamicResource ArcadeButtonPinkStyle}" Padding="14,6" Click="OnResetGamepadConfig" />
                        </StackPanel>
                        <TextBlock x:Name="GamepadConfigPath" Style="{DynamicResource ConsoleTextStyle}" FontSize="10" Opacity="0.5" />
                    </StackPanel>
                </Border>

                <!-- About -->
                <TextBlock Text="ABOUT" Style="{DynamicResource ArcadeSubtitleStyle}" Margin="0,0,0,10"/>
                <Border Style="{DynamicResource ArcadeCardPinkStyle}" Margin="0,0,0,20">
                    <StackPanel Spacing="6">
                        <TextBlock Text="HYPERSPIN EXTREME TOOLKIT" Style="{DynamicResource PixelTextStyle}" FontSize="14"/>
                        <TextBlock Text="Version 2.0  ·  C# WPF GUI + Python MCP Backend" Style="{DynamicResource ArcadeBodyStyle}" Opacity="0.65"/>
                        <TextBlock Text="Backend: mcp_bridge.py (JSON-RPC over stdio)" Style="{DynamicResource ConsoleTextStyle}" FontSize="11"/>
                        <TextBlock Text="UI: Wpf.Ui 3.x + Arcade Neon Theme" Style="{DynamicResource ConsoleTextStyle}" FontSize="11"/>
                        <TextBlock Text="MVVM: CommunityToolkit.Mvvm 8.x" Style="{DynamicResource ConsoleTextStyle}" FontSize="11"/>
                        <TextBlock Text="56/66 Milestones Complete" Style="{DynamicResource ArcadeLabelStyle}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</ui:Page>

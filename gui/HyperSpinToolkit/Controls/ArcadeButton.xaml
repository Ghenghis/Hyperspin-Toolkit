<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ══════════════════════════════════════════════════════════════
         M45 — ArcadeButton
         Neon glow on hover, pulse animation on click, sound effect trigger
         Supports mouse and gamepad interaction
         ══════════════════════════════════════════════════════════════ -->

    <Style x:Key="ArcadeButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource CabinetLightBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource NeonBlueBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource NeonBlueBrush}" />
        <Setter Property="BorderThickness" Value="1.5" />
        <Setter Property="FontFamily" Value="{DynamicResource FuturisticFont}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="20,10" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- Glow layer (behind button) -->
                        <Border x:Name="GlowBorder"
                                CornerRadius="6"
                                Background="Transparent"
                                Margin="-4"
                                Opacity="0">
                            <Border.Effect>
                                <DropShadowEffect Color="{DynamicResource NeonBlueColor}"
                                                  BlurRadius="20" ShadowDepth="0" Opacity="0.6" />
                            </Border.Effect>
                        </Border>

                        <!-- Main button body -->
                        <Border x:Name="MainBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect x:Name="ButtonShadow"
                                                  Color="{DynamicResource NeonBlueColor}"
                                                  BlurRadius="8" ShadowDepth="0" Opacity="0.2" />
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- Hover: neon glow -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="GlowBorder"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="GlowBorder"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0" Duration="0:0:0.3" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <!-- Pressed: pulse shrink -->
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="MainBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="0.95" Duration="0:0:0.05" />
                                        <DoubleAnimation Storyboard.TargetName="MainBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="0.95" Duration="0:0:0.05" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="MainBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                         To="1.0" Duration="0:0:0.15" />
                                        <DoubleAnimation Storyboard.TargetName="MainBorder"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                         To="1.0" Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <!-- Focused (gamepad) -->
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="MainBorder" Property="BorderBrush"
                                    Value="{DynamicResource NeonYellowBrush}" />
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Pink variant -->
    <Style x:Key="ArcadeButtonPinkStyle" TargetType="Button" BasedOn="{StaticResource ArcadeButtonStyle}">
        <Setter Property="Foreground" Value="{DynamicResource NeonPinkBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource NeonPinkBrush}" />
    </Style>

    <!-- Green variant -->
    <Style x:Key="ArcadeButtonGreenStyle" TargetType="Button" BasedOn="{StaticResource ArcadeButtonStyle}">
        <Setter Property="Foreground" Value="{DynamicResource NeonGreenBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource NeonGreenBrush}" />
    </Style>

    <!-- Orange variant -->
    <Style x:Key="ArcadeButtonOrangeStyle" TargetType="Button" BasedOn="{StaticResource ArcadeButtonStyle}">
        <Setter Property="Foreground" Value="{DynamicResource NeonOrangeBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource NeonOrangeBrush}" />
    </Style>

</ResourceDictionary>
